# Base SQLite locale (Prisma + adapter better-sqlite3)
DATABASE_URL="file:./dev.db"

# Secret de signature des JWT next-auth (Auth.js v5).
# Génère-le avec : npx auth secret   (ou)   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
AUTH_SECRET=""
